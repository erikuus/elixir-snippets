<snippet>
  <content><![CDATA[
def mount(_params, _session, socket) do
  socket =
    assign(socket,
      title: "${1:title}",
      description: "${2:description}"
    )

  {:ok, socket}
end
]]></content>
  <tabTrigger>.mount</tabTrigger>
  <scope>source.elixir</scope>
  <description>- Mount in live module</description>
</snippet>