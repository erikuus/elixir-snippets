<snippet>
  <content><![CDATA[
/*
def handle_event("select-item", %{"id" => id}, socket) do
  item = find_item(socket, String.to_integer(id))

  socket =
    socket
    |> assign(selected_item: item)
    |> push_event("${1:do-something}", item)

  {:noreply, socket}
end
*/    
this.handleEvent("${1:do-something}", ${2:item} => {
  {3:}
})
]]></content>
  <tabTrigger>pxlvHookHandleEvent</tabTrigger>
  <scope>source.js</scope>
  <description>- handle event in hook</description>
</snippet>
